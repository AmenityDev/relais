// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: smtp_backend.sql

package dbgen

import (
	"context"

	"github.com/google/uuid"
)

const createSMTPBackend = `-- name: CreateSMTPBackend :one
INSERT INTO smtp_backend (
    name, host, port, tls_mode, auth_user, auth_password_sealed,
    helo_name, max_concurrency, enabled
) VALUES (
    $1, $2, $3, $4, $5, $6,
    $7, $8, $9
)
RETURNING id, name, host, port, tls_mode, auth_user, auth_password_sealed, helo_name, max_concurrency, enabled, created_at, updated_at
`

type CreateSMTPBackendParams struct {
	Name               string
	Host               string
	Port               int32
	TlsMode            string
	AuthUser           string
	AuthPasswordSealed string
	HeloName           string
	MaxConcurrency     int32
	Enabled            bool
}

func (q *Queries) CreateSMTPBackend(ctx context.Context, arg CreateSMTPBackendParams) (SmtpBackend, error) {
	row := q.db.QueryRow(ctx, createSMTPBackend,
		arg.Name,
		arg.Host,
		arg.Port,
		arg.TlsMode,
		arg.AuthUser,
		arg.AuthPasswordSealed,
		arg.HeloName,
		arg.MaxConcurrency,
		arg.Enabled,
	)
	var i SmtpBackend
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Host,
		&i.Port,
		&i.TlsMode,
		&i.AuthUser,
		&i.AuthPasswordSealed,
		&i.HeloName,
		&i.MaxConcurrency,
		&i.Enabled,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteSMTPBackend = `-- name: DeleteSMTPBackend :execrows
DELETE FROM smtp_backend WHERE id = $1
`

// DeleteSMTPBackend fails while a domain still points at it, thanks to the
// ON DELETE RESTRICT foreign key. That refusal is intentional: silently
// orphaning a domain would break delivery at send time instead of at edit time.
func (q *Queries) DeleteSMTPBackend(ctx context.Context, id uuid.UUID) (int64, error) {
	result, err := q.db.Exec(ctx, deleteSMTPBackend, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getSMTPBackend = `-- name: GetSMTPBackend :one
SELECT id, name, host, port, tls_mode, auth_user, auth_password_sealed, helo_name, max_concurrency, enabled, created_at, updated_at FROM smtp_backend WHERE id = $1
`

func (q *Queries) GetSMTPBackend(ctx context.Context, id uuid.UUID) (SmtpBackend, error) {
	row := q.db.QueryRow(ctx, getSMTPBackend, id)
	var i SmtpBackend
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Host,
		&i.Port,
		&i.TlsMode,
		&i.AuthUser,
		&i.AuthPasswordSealed,
		&i.HeloName,
		&i.MaxConcurrency,
		&i.Enabled,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getSMTPBackendByName = `-- name: GetSMTPBackendByName :one
SELECT id, name, host, port, tls_mode, auth_user, auth_password_sealed, helo_name, max_concurrency, enabled, created_at, updated_at FROM smtp_backend WHERE lower(name) = lower($1::text)
`

func (q *Queries) GetSMTPBackendByName(ctx context.Context, name string) (SmtpBackend, error) {
	row := q.db.QueryRow(ctx, getSMTPBackendByName, name)
	var i SmtpBackend
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Host,
		&i.Port,
		&i.TlsMode,
		&i.AuthUser,
		&i.AuthPasswordSealed,
		&i.HeloName,
		&i.MaxConcurrency,
		&i.Enabled,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getSenderRouteForBackend = `-- name: GetSenderRouteForBackend :one
SELECT
    b.id   AS backend_id,
    b.name AS backend_name,
    b.host,
    b.port,
    b.tls_mode,
    b.auth_user,
    b.auth_password_sealed,
    b.helo_name,
    b.max_concurrency,
    b.enabled
FROM smtp_backend b
WHERE b.id = $1
`

type GetSenderRouteForBackendRow struct {
	BackendID          uuid.UUID
	BackendName        string
	Host               string
	Port               int32
	TlsMode            string
	AuthUser           string
	AuthPasswordSealed string
	HeloName           string
	MaxConcurrency     int32
	Enabled            bool
}

// GetSenderRouteForBackend loads the relay a message was pinned to at ingestion.
//
// The worker deliberately does not re-resolve the sender's domain: the backend
// was chosen and recorded when the message was accepted (D6), so an admin
// re-assigning the domain in the meantime must not silently redirect mail that
// was already queued for somewhere else.
func (q *Queries) GetSenderRouteForBackend(ctx context.Context, backendID uuid.UUID) (GetSenderRouteForBackendRow, error) {
	row := q.db.QueryRow(ctx, getSenderRouteForBackend, backendID)
	var i GetSenderRouteForBackendRow
	err := row.Scan(
		&i.BackendID,
		&i.BackendName,
		&i.Host,
		&i.Port,
		&i.TlsMode,
		&i.AuthUser,
		&i.AuthPasswordSealed,
		&i.HeloName,
		&i.MaxConcurrency,
		&i.Enabled,
	)
	return i, err
}

const listSMTPBackends = `-- name: ListSMTPBackends :many
SELECT id, name, host, port, tls_mode, auth_user, auth_password_sealed, helo_name, max_concurrency, enabled, created_at, updated_at FROM smtp_backend ORDER BY name
`

func (q *Queries) ListSMTPBackends(ctx context.Context) ([]SmtpBackend, error) {
	rows, err := q.db.Query(ctx, listSMTPBackends)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SmtpBackend{}
	for rows.Next() {
		var i SmtpBackend
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Host,
			&i.Port,
			&i.TlsMode,
			&i.AuthUser,
			&i.AuthPasswordSealed,
			&i.HeloName,
			&i.MaxConcurrency,
			&i.Enabled,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSMTPBackendsNeedingRewrap = `-- name: ListSMTPBackendsNeedingRewrap :many
SELECT id, name, auth_password_sealed
FROM smtp_backend
WHERE auth_password_sealed <> ''
ORDER BY name
`

type ListSMTPBackendsNeedingRewrapRow struct {
	ID                 uuid.UUID
	Name               string
	AuthPasswordSealed string
}

func (q *Queries) ListSMTPBackendsNeedingRewrap(ctx context.Context) ([]ListSMTPBackendsNeedingRewrapRow, error) {
	rows, err := q.db.Query(ctx, listSMTPBackendsNeedingRewrap)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListSMTPBackendsNeedingRewrapRow{}
	for rows.Next() {
		var i ListSMTPBackendsNeedingRewrapRow
		if err := rows.Scan(&i.ID, &i.Name, &i.AuthPasswordSealed); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateSMTPBackend = `-- name: UpdateSMTPBackend :one
UPDATE smtp_backend
SET name                 = $1,
    host                 = $2,
    port                 = $3,
    tls_mode             = $4,
    auth_user            = $5,
    auth_password_sealed = CASE WHEN $6::boolean
                                THEN $7
                                ELSE auth_password_sealed END,
    helo_name            = $8,
    max_concurrency      = $9,
    enabled              = $10
WHERE id = $11
RETURNING id, name, host, port, tls_mode, auth_user, auth_password_sealed, helo_name, max_concurrency, enabled, created_at, updated_at
`

type UpdateSMTPBackendParams struct {
	Name               string
	Host               string
	Port               int32
	TlsMode            string
	AuthUser           string
	RotatePassword     bool
	AuthPasswordSealed string
	HeloName           string
	MaxConcurrency     int32
	Enabled            bool
	ID                 uuid.UUID
}

// UpdateSMTPBackend leaves the sealed password untouched when
// @rotate_password is false, so an admin editing the host does not have to
// re-enter the credential.
func (q *Queries) UpdateSMTPBackend(ctx context.Context, arg UpdateSMTPBackendParams) (SmtpBackend, error) {
	row := q.db.QueryRow(ctx, updateSMTPBackend,
		arg.Name,
		arg.Host,
		arg.Port,
		arg.TlsMode,
		arg.AuthUser,
		arg.RotatePassword,
		arg.AuthPasswordSealed,
		arg.HeloName,
		arg.MaxConcurrency,
		arg.Enabled,
		arg.ID,
	)
	var i SmtpBackend
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Host,
		&i.Port,
		&i.TlsMode,
		&i.AuthUser,
		&i.AuthPasswordSealed,
		&i.HeloName,
		&i.MaxConcurrency,
		&i.Enabled,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateSMTPBackendSealedPassword = `-- name: UpdateSMTPBackendSealedPassword :exec
UPDATE smtp_backend
SET auth_password_sealed = $1
WHERE id = $2
`

type UpdateSMTPBackendSealedPasswordParams struct {
	AuthPasswordSealed string
	ID                 uuid.UUID
}

func (q *Queries) UpdateSMTPBackendSealedPassword(ctx context.Context, arg UpdateSMTPBackendSealedPasswordParams) error {
	_, err := q.db.Exec(ctx, updateSMTPBackendSealedPassword, arg.AuthPasswordSealed, arg.ID)
	return err
}
